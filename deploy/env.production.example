# Memo App Backend - Production .env example
# Copy to memwebapp/backend/.env on EC2 and fill in values.

PORT=8002
NODE_ENV=production

# Use your EC2 public URL or domain (no trailing slash)
FRONTEND_URL=https://your-domain.com
DASHBOARD_BASE_URL=https://your-domain.com

# Database (e.g. RDS PostgreSQL)
DATABASE_URL=postgresql://user:password@your-rds-endpoint:5432/memoapp

# JWT (generate a strong secret)
JWT_SECRET=your-production-jwt-secret

# Optional: OAuth (Google/Microsoft) - use your production redirect URIs
# GOOGLE_REDIRECT_URI=https://your-domain.com/api/v1/web/auth/google/callback
# MICROSOFT_REDIRECT_URI=https://your-domain.com/api/v1/web/auth/microsoft/callback
